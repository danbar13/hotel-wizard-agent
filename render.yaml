services:
  - type: web
    name: whatsapp-service-agent
    env: node
    plan: free
    buildCommand: ""
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
